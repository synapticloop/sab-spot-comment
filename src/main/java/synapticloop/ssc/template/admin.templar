<!doctype html>
<html lang="en">
<head>
	<title>SabSpotComment - Admin</title>
	<link rel="stylesheet" href="/static/css/reset.css" />
	<link rel="stylesheet" href="/static/css/style.css" />
	<script src="//ajax.googleapis.com/ajax/libs/jquery/2.0.3/jquery.min.js"></script>
	<link rel="icon" href="/static/images/routemaster.png" type="image/png" />
</head>
<body>
<div id="main">
	<h1>SabSpotComment<em> ADMIN</em></h1>
	<ul class="nav">
		<li><a href="/">&laquo; home &raquo;</a>
		<li class="current">&laquo; admin &raquo;</li>
	</ul>

	{if isSabNzbSetup}
		{if isNewznabSetup}
			<h2 class="ok">SabSpotComment is setup and ready to go!</h2>
		{endif}
	{endif}

	<form method="post">
		<h2>SabNzbd server</h2>

		<div class="section">
			{if isSabNzbSetup}
				<p class="ok">sabnzbd: Everything is looking good</p>
			{else}
				<p class="warn">sabnzbd: Could not connect, please check the values below:</p>
			{endif}

			{if fn:notNull[sabNzbErrorMessage]}
				<p class="error">{sabNzbErrorMessage}</p>
			{endif}

			SABNZB URL ------&gt;<input type="text" name="sabNzbUrl" id="sabNzbUrl" value="{sabNzbUrl}" size="40" /><br />
			SABNZB API KEY --&gt;<input type="text" name="sabNzbApiKey" id="sabNzbApiKey" value="{sabNzbApiKey}" size="40" /><br />

			{loop newznabServers as newznabServer}
				{if newznabServerStatus.first}
					<p class="ok">SabNzbd registered news hosts:</p>
					<ol>
				{endif}
				<li class="ok">{newznabServer}</li>
				{if newznabServerStatus.last}
					</ol>
				{endif}
			{endloop}
		</div>

		<h2>Newznab server</h2>

		<div class="section">
			{if isNewznabSetup}
				<p class="ok">newznab: Everything is looking good</p>
			{else}
				<p class="warn">newznab: Could not connect, please check the values below:</p>
			{endif}
	
			{if fn:notNull[newznabErrorMessage]}
				<p class="error">{newznabErrorMessage}</p>
			{endif}

			<p>Enter your newznab url, this should be of the form <em>https://newznab.example.com</em></p>
			NEWZNAB URL -----&gt;<input type="text" name="newznabUrl" id="newznabUrl" value="{newznabUrl}" size="40" /><br />
			<p>Enter your newznab api key.  Go to your user account and it will be listed under <em>Site Api/Rss Key</em></p>
			NEWZNAB API KEY -&gt;<input type="text" name="newznabApiKey" id="newznabApiKey" value="{newznabApiKey}" size="40" /><br />
		</div>

		<h2>Timings</h2>

		<div class="section">

			<p>Don't post a comment if any of the following conditions are true</p>

			<input type="submit" name="submit" value="submit" />
		</div>
	</form>

	<div id="footer">
		<p>powered by: synapticloop - sabspotcomment - routemaster - nanohttpd</p>
	</div>

</div>
</body>
</html>
